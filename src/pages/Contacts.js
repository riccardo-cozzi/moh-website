import React, { useContext, useState } from 'react';
import { 
    Grid, 
    Box,
    TextField
} from '@mui/material';

const Contacts = () => {
    return <>
        <Box sx={{background:"#efe", borderRadius: 5, justifyContent: "center", alignItems: "center", display: "flex", height: "50vh"}}>
            <h1>Contacts</h1>
        </Box>
    </>
}

export default Contacts;